/ This list page should only appear for admin
%h1 Listing workouts

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @workouts.each do |workout|
      %tr
        %td= link_to "#{workout.created_at.strftime("%a, %b %d %Y")}", edit_workout_path(workout) 
        %td
          =" . . . . "
        %td= link_to 'Destroy', workout, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Workout', new_workout_path
