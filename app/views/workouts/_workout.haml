- cache workout do
  #notification.card{"data-controller" => "timer", "data-timer-workout-created" => "#{workout.created_at.to_i}", "data-timer-workout-completed" => "#{workout.completed_at.to_i}", class: ("archived" if workout.active == false)}

    .column
      .columns.col-12
      .columns.col-12{style: "text-align: center;"}
        .col-1
        .col-3
          -if workout.active
            Rest Time:
            %br
            %span#settTimer{"data-target" => "timer.sett", "data-action" => "timer->sett#setTimer"}
              0:00
          -else
            Archived workout
        .col-5
          Workout: 
          %br
          %span#workoutTimer{"data-target" => "timer.workout", "data-action" => "timer->sett#workoutTimer"}
            0:00
        .col-3.right
          -if workout.active
            %span.complete-workout.btn
              %a{href: '/workouts/stop'}
                Finish
          -else
            / %span.complete-workout.btn
            /   %a{href: '/workouts/resume'}
            /     Edit

  .card{class: ("inactive" if workout.active == false)}
    .card-header
      .card-title.h5.column
        .columns
          .col-12
            =link_to workout.formated_date, edit_workout_path(workout)
            
      .card-subtitle.text-gray
      .card-body.columns
        - workout.unique_exercises.each do |exercise|
          .columns{style: "border-bottom: 2px solid white"}
            %h5
              = exercise.name
          .column.col-12{style: "font-size: 15pt"}
            - workout.setts.where(exercise_id: exercise.id).order(:id).each_with_index do |sett, index|
              .columns.col-12{"data-controller" => "sett", "data-sett-id" => sett.id, "data-sett-active" => "#{workout.active.to_s}", "data-sett-completed-at" => workout.completed_at}
              
              
                .left.col-3
                  %p
                    ="Set #{index+1} "
                .col-3
                  %p
                    ="#{sett.current_weight} lbs"
                    / %small.tile-subtitle.text-gray
                    /   %input{type: "number", min: 0, max: 2000, step: 2.5, value: sett.current_weight, "data-target" => "sett.weight", "data-action" => "change->sett#updateWeight"}

                .col-1{"data-controller" => "video"}
                  -if ENV['FEATURE_VIDEO']
                    %button.btn.btn{"data-target" => "video.button", "data-value" => "0", "data-action" => "click->video#toggleVideoRecord"}
                      .icon.icon-check
                    
                .right.col-5
                  %button.btn.btn{"data-target" => "sett.button", "data-action" => "click->sett#updateReps", :class => ("btn-primary-light-red" if sett.reps_completed < sett.reps_goal)}
                    .div{"data-target" => "sett.reps", "value" => "0"}
                      ="#{sett.reps_completed} x #{sett.reps_goal}"

      .card-footer
      / #gUMArea
      /   %div
      /     Record:
      /     = succeed "Video" do
      /       %input#mediaVideo{:checked => "checked", :name => "media", :type => "radio", :value => "video"}/
      /     = succeed "audio" do
      /       %input{:name => "media", :type => "radio", :value => "audio"}/
      /   %button#gUMbtn.btn.btn-default Request Stream
      / #btns
      /   %button#start.btn.btn-default Start
      /   %button#stop.btn.btn-default Stop
      / %div
      /   %ul#ul.list-unstyled
